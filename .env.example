# ── Backend API URL ───────────────────────────────────────────────────────────
# Leave empty to use local-first (SQLite only, no backend)
# Set to your Render backend URL to enable real auth + cloud sync
# e.g. https://kharchaaa-backend.onrender.com
EXPO_PUBLIC_API_URL=
